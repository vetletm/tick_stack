HOST=<%= scope['tick_stack::chronograf::host'] %>
PORT=<%= scope['tick_stack::chronograf::port'] %>
<% if scope['tick_stack::chronograf::influxdb_url'] %>
INFLUXDB_URL=<%= scope['tick_stack::chronograf::influxdb_url'] %>
<% end %>
<% if scope['tick_stack::chronograf::influxdb_username'] %>
INFLUXDB_USERNAME=<%= scope['tick_stack::chronograf::influxdb_username'] %>
<% end %>
<% if scope['tick_stack::chronograf::influxdb_password'] %>
INFLUXDB_PASSWORD=<%= scope['tick_stack::chronograf::influxdb_password'] %>
<% end %>
<% if scope['tick_stack::chronograf::kapacitor_url'] %>
KAPACITOR_URL=<%= scope['tick_stack::chronograf::kapacitor_url'] %>
<% end %>
<% if scope['tick_stack::chronograf::kapacitor_username'] %>
KAPACITOR_USERNAME=<%= scope['tick_stack::chronograf::kapacitor_username'] %>
 <% end %>
<% if scope['tick_stack::chronograf::kapacitor_password'] %>
KAPACITOR_PASSWORD=<%= scope['tick_stack::chronograf::kapacitor_password'] %>
<% end %>
<% if scope['tick_stack::chronograf::cert'] %>
TLS_CERTIFICATE=<%= scope['tick_stack::chronograf::cert'] %>
TLS_PRIVATE_KEY=<%= scope['tick_stack::chronograf::key'] %>
<% end %>
