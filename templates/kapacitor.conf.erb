# kapacitor Configuration
#
# THIS FILE IS MANAGED BY PUPPET
#

hostname = "<%= scope['tick_stack::kapacitor::hostname'] %>"
data_dir = "<%= scope['tick_stack::kapacitor::data_dir'] %>"

<% if scope['tick_stack::kapacitor::http'] -%>
[http]
<%   scope['tick_stack::kapacitor::http'].each do |key, value| -%>
  <%= key %> = <%= value %>
<%   end -%>
<% end -%>

<% if scope['tick_stack::kapacitor::replay'] -%>
[replay]
<%   scope['tick_stack::kapacitor::replay'].each do |key, value| -%>
  <%= key %> = <%= value %>
<%   end -%>
<% end -%>

<% if scope['tick_stack::kapacitor::storage'] -%>
[storage]
<%   scope['tick_stack::kapacitor::storage'].each do |key, value| -%>
  <%= key %> = <%= value %>
<%   end -%>
<% end -%>

<% if scope['tick_stack::kapacitor::task'] -%>
[task]
<%   scope['tick_stack::kapacitor::task'].each do |key, value| -%>
  <%= key %> = <%= value %>
<%   end -%>
<% end -%>

<% if scope['tick_stack::kapacitor::load'] -%>
[load]
<%   scope['tick_stack::kapacitor::load'].each do |key, value| -%>
  <%= key %> = <%= value %>
<%   end -%>
<% end -%>

<% if scope['tick_stack::kapacitor::logging'] -%>
[logging]
<%   scope['tick_stack::kapacitor::logging'].each do |key, value| -%>
  <%= key %> = <%= value %>
<%   end -%>
<% end -%>

<% if scope['tick_stack::kapacitor::influxdb'] -%>
[[influxdb]]
<%   scope['tick_stack::kapacitor::influxdb'].each do |key, value| -%>
  <%= key %> = <%= value %>
<%   end -%>
<% end -%>
